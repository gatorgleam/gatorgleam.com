---
// Professional Footer Component - SeaForth Strategies
// Teal Background with Gold & White Accents
---

<footer class="footer">
  <div class="footer-content">
    <div class="container">
      <!-- Logo -->
      <div class="footer-logo">
        <img 
          src="/images/logo.png" 
          alt="SeaForth Strategies" 
          class="logo-image"
          width="200"
          height="50"
          loading="lazy"
        />
      </div>

      <!-- Tagline -->
      <p class="footer-tagline">
        Where Strategy Meets Storytelling
      </p>

      <!-- Contact Info -->
      <div class="footer-contact">
        <a href="mailto:contact@seaforthstrategies.com" class="contact-link">
          <i class="fas fa-envelope"></i>
          contact@seaforthstrategies.com
        </a>
        <a 
          href="https://calendly.com/gatorgleamsmm/30min" 
          target="_blank" 
          rel="noopener noreferrer"
          class="contact-link"
        >
          <i class="fas fa-calendar-alt"></i>
          Schedule a Consultation
        </a>
      </div>

      <!-- Social Media -->
      <div class="footer-social">
        <a 
          href="https://www.instagram.com/seaforthstrategies/" 
          target="_blank" 
          rel="noopener noreferrer"
          aria-label="Follow us on Instagram"
          class="social-link"
        >
          <i class="fab fa-instagram"></i>
        </a>
        <a 
          href="https://www.linkedin.com/company/seaforthstrategies/" 
          target="_blank" 
          rel="noopener noreferrer"
          aria-label="Connect on LinkedIn"
          class="social-link"
        >
          <i class="fab fa-linkedin"></i>
        </a>
        <a 
          href="https://x.com/seaforthco" 
          target="_blank" 
          rel="noopener noreferrer"
          aria-label="Follow us on X"
          class="social-link"
        >
          <i class="fab fa-x-twitter"></i>
        </a>
      </div>

      <!-- Navigation Links -->
      <nav class="footer-nav" aria-label="Footer navigation">
        <a href="/">Home</a>
        <span class="separator">•</span>
        <a href="/about">About</a>
        <span class="separator">•</span>
        <a href="/services">Services</a>
        <span class="separator">•</span>
        <a href="/contact">Contact</a>
      </nav>

      <!-- Copyright -->
      <div class="footer-bottom">
        <p class="copyright">
          © {new Date().getFullYear()} SeaForth Strategies. All rights reserved.
        </p>
        <p class="credit">
          Crafted with <span class="heart">♥</span> in Southern California
        </p>
      </div>
    </div>
  </div>

  <!-- Wave Divider at Top -->
  <div class="wave-divider-top">
    <svg viewBox="0 0 1200 120" preserveAspectRatio="none">
      <path d="M321.39,56.44c58-10.79,114.16-30.13,172-41.86,82.39-16.72,168.19-17.73,250.45-.39C823.78,31,906.67,72,985.66,92.83c70.05,18.48,146.53,26.09,214.34,3V0H0V27.35A600.21,600.21,0,0,0,321.39,56.44Z" fill="#2A7C84"></path>
    </svg>
  </div>
</footer>

<style>
  /* =============================================================================
     FOOTER STYLES - Teal Background with Gold & White
     ============================================================================= */
  .footer {
    position: relative;
    background: linear-gradient(135deg, #2A7C84 0%, #1F5C62 100%);
    color: var(--color-white);
    margin-top: var(--space-5xl);
    padding: var(--space-4xl) 0 var(--space-2xl);
    overflow: hidden;
  }

  .footer::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 
      radial-gradient(circle at 20% 50%, rgba(213, 163, 83, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 80%, rgba(255, 255, 255, 0.05) 0%, transparent 50%);
    z-index: 0;
  }

  .footer-content {
    position: relative;
    z-index: 1;
    text-align: center;
  }

  /* Logo */
  .footer-logo {
    margin-bottom: var(--space-lg);
  }

  .logo-image {
    max-width: 250px;
    height: auto;
    filter: brightness(0) invert(1);
    margin: 0 auto;
    opacity: 0.95;
  }

  /* Tagline */
  .footer-tagline {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--color-gold);
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    margin: 0 auto var(--space-xl);
    text-align: center;
    width: 100%;
  }

  /* Contact Links */
  .footer-contact {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
  }

  .contact-link {
    color: var(--color-white);
    text-decoration: none;
    font-size: 1rem;
    transition: all var(--transition-base);
    display: inline-flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .contact-link:hover {
    color: var(--color-gold);
    transform: translateY(-2px);
  }

  .contact-link i {
    font-size: 1.125rem;
    color: var(--color-gold);
    transition: transform var(--transition-base);
  }

  .contact-link:hover i {
    transform: scale(1.1);
  }

  /* Social Media Icons */
  .footer-social {
    display: flex;
    justify-content: center;
    gap: var(--space-lg);
    margin-bottom: var(--space-xl);
  }

  .social-link {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-white);
    text-decoration: none;
    transition: all var(--transition-base);
    border: 2px solid rgba(255, 255, 255, 0.2);
  }

  .social-link:hover {
    background: var(--color-gold);
    border-color: var(--color-gold);
    transform: translateY(-4px);
    box-shadow: 0 8px 20px rgba(213, 163, 83, 0.4);
  }

  .social-link i {
    font-size: 1.25rem;
  }

  /* Footer Navigation */
  .footer-nav {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: var(--space-md);
    flex-wrap: wrap;
    margin-bottom: var(--space-xl);
  }

  .footer-nav a {
    color: var(--color-white);
    text-decoration: none;
    font-size: 1rem;
    font-weight: 500;
    transition: color var(--transition-base);
  }

  .footer-nav a:hover {
    color: var(--color-gold);
  }

  .separator {
    color: rgba(255, 255, 255, 0.4);
    font-size: 0.875rem;
  }

  /* Footer Bottom */
  .footer-bottom {
    border-top: 1px solid rgba(255, 255, 255, 0.2);
    padding-top: var(--space-lg);
    margin-top: var(--space-xl);
    text-align: center;
  }

  .copyright,
  .credit {
    font-size: 0.9375rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0 auto var(--space-sm);
    text-align: center;
    width: 100%;
    display: block;
  }

  .heart {
    color: var(--color-gold);
    display: inline-block;
    animation: heartbeat 1.5s ease-in-out infinite;
  }

  @keyframes heartbeat {
    0%, 100% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.1);
    }
  }

  /* Wave Divider at Top */
  .wave-divider-top {
    position: absolute;
    top: -1px;
    left: 0;
    width: 100%;
    overflow: hidden;
    line-height: 0;
    transform: rotate(180deg);
  }

  .wave-divider-top svg {
    position: relative;
    display: block;
    width: calc(100% + 1.3px);
    height: 80px;
  }

  /* =============================================================================
     MOBILE RESPONSIVE
     ============================================================================= */
  @media (max-width: 768px) {
    .footer {
      padding: var(--space-3xl) 0 var(--space-xl);
      margin-top: var(--space-3xl);
    }

    .logo-image {
      max-width: 200px;
    }

    .footer-tagline {
      font-size: 0.9375rem;
    }

    .footer-contact {
      gap: var(--space-sm);
    }

    .contact-link {
      font-size: 0.9375rem;
    }

    .footer-social {
      gap: var(--space-md);
    }

    .social-link {
      width: 44px;
      height: 44px;
    }

    .social-link i {
      font-size: 1.125rem;
    }

    .footer-nav {
      flex-direction: column;
      gap: var(--space-sm);
    }

    .separator {
      display: none;
    }

    .wave-divider-top svg {
      height: 50px;
    }
  }
</style>
